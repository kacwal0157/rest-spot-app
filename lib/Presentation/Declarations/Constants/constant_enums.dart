enum EditWidget {
  tableLayout,
  fontEditor,
  colorEditor,
  languageEditor,
  contentAmountEditor,
  contentEditor,
  saveChanges,
}

enum ContentPage {
  main,
  category,
  dish,
  ingredient,
}

enum EditingPage {
  main,
  card,
}

enum ImagePage {
  main,
  category,
  dish,
}

enum Languages {
  pl,
  gb,
  de,
  es,
}
